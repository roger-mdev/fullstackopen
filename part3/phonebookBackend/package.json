{
  "name": "phonebookbackend",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "roger",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../part2/The-Phonebook && npm run build && cp -r dist ../../part3/phonebookbackend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.8.1",
    "morgan": "^1.11.0"
  }
}
